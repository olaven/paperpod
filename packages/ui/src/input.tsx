import { styled } from "./stitches.config";

export const Input = styled("input", {
    border: 'solid 1px',
    padding: "$three",
    margin: "$three",
    fontSize: "$twentyone",
    color: "$secondary",
    borderRadius: "5px"
});
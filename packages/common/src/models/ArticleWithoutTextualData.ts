export type ArticleWithoutTextualData = {
    text: string,
    title: string,
    author: string,
    description: string,
    publication_timestamp: number,
    original_url: string,
    _id: string
    owner_id: string
    added_timestamp: number
    storage_uri: string
}